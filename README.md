# XMU-FTP-Checker
用于检查厦门大学信息学院FTP上是否有你没交的作业

## 一、使用方法
1. 填写FTP地址和登录信息
2. 填写你需要跟踪的作业文件夹的FTP路径
3. 填写你的名字
4. 单击“保存并检查”

## 二、判断逻辑
1. 跟踪路径中存在具有三个以上文件的子文件夹时，若其中任意一个文件的文件名中包含与key完全匹配的字段，输出该子文件夹路径并标注“已完成”
2. 跟踪路径中存在具有三个以上文件的子文件夹时，若其中所有文件的文件名中均不包含与key完全匹配的字段，输出该子文件夹路径并标注“未完成”
3. 跟踪路径子文件夹中文件数小于等于3时不进行处理
